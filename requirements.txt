# Core dependencies first - install with: pip install --extra-index-url https://download.pytorch.org/whl/cu124 -r requirements.txt
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
numpy==1.24.3

# ML dependencies that depend on torch
xformers==0.0.28.post3  # Compatible with torch==2.5.1
einops
transformers==4.46.0
accelerate>=0.25.0  # Required for proper model loading and memory management

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Image processing
Pillow==10.1.0
opencv-python==4.8.1.78

# Text processing (required by transformers)
ftfy

# WAN 2.2 T2V and I2V models - requires diffusers main branch
git+https://github.com/huggingface/diffusers
# FaceFusion - will be cloned automatically on server startup
