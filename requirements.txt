# Core dependencies first - install with: pip install --extra-index-url https://download.pytorch.org/whl/nightly/cu128 -r requirements.txt
# PyTorch NIGHTLY with CUDA 12.8+ support for Blackwell GPUs (sm_120)
--pre
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
torch
torchvision
torchaudio
numpy==2.2.4

# ML dependencies that depend on torch
# Skip xformers for now - it takes too long to compile from source
# xformers  # Nightly build to match PyTorch nightly - DISABLED due to long compilation
einops
transformers==4.46.0
accelerate>=0.25.0  # Required for proper model loading and memory management

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Image processing
Pillow==10.1.0
opencv-python==4.11.0.86


# Video processing (recommended for better video export)
imageio
imageio-ffmpeg

# FaceFusion dependencies
onnxruntime==1.22.0
onnx==1.17.0
gradio==5.25.2
gradio-rangeslider==0.0.8
psutil==7.0.0
tqdm==4.67.1
scipy==1.15.2

# Text processing (required by transformers)
ftfy

# WAN 2.2 T2V and I2V models - requires diffusers main branch
git+https://github.com/huggingface/diffusers
# FaceFusion - will be cloned automatically on server startup
